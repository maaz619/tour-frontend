const baseUrl: string = `http://localhost:9000/api/v1`
export const tourUrl: string = `${baseUrl}/tours`
export const loginUrl: string = `${baseUrl}/users/login`
export const profileUrl: string = `${baseUrl}/users/me`
